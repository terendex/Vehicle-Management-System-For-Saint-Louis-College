# The canonical pin list moved to the repository root — see ../requirements.txt
# for why (Railpack's install step only receives the dependency file it detected
# at the root, so a list here is not present when pip runs).
#
# This file is kept so the documented commands still work unchanged:
#     pip install -r backend/requirements.txt      (from the repo root)
#     pip install -r requirements.txt              (from inside backend/)
#
# pip resolves a nested -r relative to the file containing it, so this points at
# the root list from either working directory.
-r ../requirements.txt
